<?xml version="1.0" encoding="UTF-8"?>
<!--
  ============================================================================
   PharmaTrack - Pharmacy Inventory and Sales Management System
   SWC4253 / SWC4593 Enterprise Software Development - Group Project
   Universiti Poly-Tech Malaysia (UPTM)
  ============================================================================
   BUILD OWNER : Aliff (Architecture)
   Do not edit this file without telling the group - a broken pom.xml stops
   everyone from building. Open an issue instead if you need a new library.
  ============================================================================
   TARGET SERVER : Apache Tomcat 9.x  /  GlassFish 5-6   (Java EE 8)
   NAMESPACE     : javax.servlet.*   (NOT jakarta.servlet.* - see README)
   JAVA VERSION  : 1.8
  ============================================================================
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <groupId>my.edu.uptm</groupId>
    <artifactId>pharmatrack</artifactId>
    <version>1.0</version>
    <packaging>war</packaging>

    <name>PharmaTrack</name>
    <description>Pharmacy Inventory and Sales Management System (Java EE)</description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.source>1.8</maven.compiler.source>
        <maven.compiler.target>1.8</maven.compiler.target>
        <failOnMissingWebXml>false</failOnMissingWebXml>
    </properties>

    <dependencies>

        <!-- ================================================================
             Servlet API (Java EE 8 / javax namespace).
             scope=provided  ->  supplied by Tomcat at runtime, NOT packaged.
             ================================================================ -->
        <dependency>
            <groupId>javax.servlet</groupId>
            <artifactId>javax.servlet-api</artifactId>
            <version>4.0.1</version>
            <scope>provided</scope>
        </dependency>

        <!-- JSP API - also provided by the container -->
        <dependency>
            <groupId>javax.servlet.jsp</groupId>
            <artifactId>javax.servlet.jsp-api</artifactId>
            <version>2.3.3</version>
            <scope>provided</scope>
        </dependency>

        <!-- ================================================================
             JSTL - used in every .jsp for loops / conditionals.
             This one IS packaged into the .war (scope defaults to compile).
             ================================================================ -->
        <dependency>
            <groupId>javax.servlet</groupId>
            <artifactId>jstl</artifactId>
            <version>1.2</version>
        </dependency>

        <!-- ================================================================
             MySQL JDBC driver - Ramzi's module (database connectivity).
             Version 8.0.33 works with MySQL 5.7 and 8.x.
             ================================================================ -->
        <dependency>
            <groupId>mysql</groupId>
            <artifactId>mysql-connector-java</artifactId>
            <version>8.0.33</version>
        </dependency>

        <!-- ================================================================
             jBCrypt - password hashing. Aliff's module (security).
             Rubric requires "encrypted password storage" - plain text or
             plain MD5/SHA1 will lose marks. BCrypt is salted + slow by
             design, which is exactly what is wanted here.
             ================================================================ -->
        <dependency>
            <groupId>org.mindrot</groupId>
            <artifactId>jbcrypt</artifactId>
            <version>0.4</version>
        </dependency>

        <!-- ================================================================
             JUnit - optional, for anyone who wants to unit test their DAO.
             Not required by the rubric but looks good in the report.
             ================================================================ -->
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.13.2</version>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <finalName>pharmatrack</finalName>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
                <configuration>
                    <source>1.8</source>
                    <target>1.8</target>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-war-plugin</artifactId>
                <version>3.3.2</version>
            </plugin>
        </plugins>
    </build>

</project>
